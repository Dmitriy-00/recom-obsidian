# Taxonomies

Semantic relationships between registry entries. No media instances should appear here.

## Example structure
- `genre-theme.csv` — maps genres to relevant themes.
- `trope-cognitive.csv` — maps tropes to cognitive operations they typically trigger.

Each CSV should include `source`, `target`, and `relation` columns to keep relations explicit and explainable.
