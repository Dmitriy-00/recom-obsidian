# System Guidelines

- Obsidian vault is the canonical source of truth; Markdown + YAML frontmatter only.
- Keep layers separate: registries → taxonomies → media → users → logic → views → export.
- Favor explainability: every score should be decomposable into explicit components.
- CSV files mirror the YAML data model for migration to SQL/APIs.
